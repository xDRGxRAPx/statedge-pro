{
  "buildCommand": "pnpm --filter @workspace/statedge run build && cp -r artifacts/statedge/dist ./dist",
  "outputDirectory": "dist",
  "installCommand": "pnpm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
