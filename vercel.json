{
    "buildCommand": "pnpm install && pnpm --filter @workspace/statedge run build",
    "outputDirectory": "artifacts/statedge/dist",
    "installCommand": "pnpm install",
    "framework": null,
    "rewrites": [
      { "source": "/(.*)", "destination": "/index.html" }
    ]
  }
  